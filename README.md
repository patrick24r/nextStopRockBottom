Usage:
Create a `config.json` file with the following format in the 
```
{
  "stops": ["stop_number_1", "stop_number_2"]
}
```

Build:
```
bazel build //:next-stop-rock-bottom
```

Run
```
bazel run //:next-stop-rock-bottom
```